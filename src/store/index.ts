import { createStore } from 'vuex'
import breadcrumbModule from './breadcrumb'

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        breadcrumbModule
    }
})
